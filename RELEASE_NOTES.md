# VoiceToText v1.0.0 发布说明

本地轻量 Windows 应用：录音 / 选择音频文件 → 线上高精度识别 → 文字实时显示、复制、导出 `txt` / `srt` 字幕。面向**上课录音、开会录音**场景。

## 主要功能

- **三后端可切换**：讯飞听写、讯飞转写（LFASR，支持说话人分离）、百度语音、OpenAI Whisper。哪家有 Key 用哪家，可在界面下拉随时切换。
- **本地降噪增强**：发送识别前做一次轻量降噪（频谱门控 + 80Hz 高通），抑制教室 / 会议室空调、风扇等背景噪音。
- **说话人分离**：使用「讯飞转写」后端时，可区分不同说话人（需在讯飞控制台为应用开通「录音文件转写(LFASR)」服务）。
- **双语双文件**：选「双语(中+英)」时，同一段音频分别生成**纯中文**与**纯英文**两份独立结果，导出为 `基名_zh` / `基名_en` 的 txt 与 srt。
- **界面本地化**：支持中文 / English 一键切换；主题支持亮色 / 暗色 / 随系统（跟随 Windows 深色模式）。
- **设置窗口**：按所选后端只显示对应 Key 输入框，窗口可自由拖动。

## 安装

下载 Release 中的 `VoiceToText-1.0.0-windows.zip`，解压得到 `VoiceToText-Setup-1.0.0.exe`，双击按向导安装（默认装到用户程序目录，无需管理员权限，会创建开始菜单与桌面快捷方式）。

> 若首次启动提示缺少 `vcruntime140.dll` 等运行库，请安装
> [Visual C++ 2015–2022 运行库](https://learn.microsoft.com/zh-CN/cpp/windows/latest-supported-vc-redist)。

## 使用

1. 启动后点「设置API key」，填入讯飞 / 百度 / OpenAI 任一家的 Key 并保存（密钥仅存本机 `%APPDATA%\VoiceToText\config.json`）。
2. 选择识别引擎与语言（中文 / 英文 / 自动 / 双语）。
3. 点「开始录音」录音，或「选择音频文件」导入 `wav / mp3 / flac / m4a` 等，再点「转写」。
4. 结果可复制，或导出 `.txt` / `.srt`。

## 说明

- 识别由线上服务完成，本地不含语音模型，安装包约 24 MB。
- 需要联网调用识别服务；纯离线场景不适用。
- 音频仅发送至你选择的后端服务，本地不留存识别内容（导出文件除外）。
- 若杀毒软件误报，请将程序加入白名单。
